language: r

os:
 - linux
 - osx

dist: xenial

cache: packages

latex: true

r_github_packages:
  - r-lib/pkgdown
  
r_packages:
  - covr
     
after_success:
  - Rscript -e 'pkgdown::build_site()'
  - Rscript -e 'library(covr); codecov()'
      
deploy:
  provider: pages
  skip_cleanup: true
  token: $GITHUB_PAT
  keep_history: false
  local_dir: docs
  on:
    branch: master
